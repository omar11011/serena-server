module.exports = specie => {

    const moves = []

    if (specie === "charizard") moves.push({ name: "gigallamarada" })
    if (specie === "butterfree") moves.push({ name: "gigaestupor" })
    if (specie === "pikachu") moves.push({ name: "gigatronada" })
    if (specie === "meowth") moves.push({ name: "gigamonedas" })
    if (specie === "machamp") moves.push({ name: "gigapunicion" })
    if (specie === "gengar") moves.push({ name: "gigaaparaicion" })
    if (specie === "kingler") moves.push({ name: "gigaespuma" })
    if (specie === "lapras") moves.push({ name: "gigamelodia" })
    if (specie === "eevee") moves.push({ name: "gigaternura" })
    if (specie === "snorlax") moves.push({ name: "gigarreciclaje" })
    if (specie === "garbodor") moves.push({ name: "gigapestilencia" })
    if (specie === "melmetal") moves.push({ name: "gigafundido" })
    if (specie === "corviknight") moves.push({ name: "gigahuracan" })
    if (specie === "orbeetle") moves.push({ name: "gigaboveda" })
    if (specie === "drednaw") moves.push({ name: "gigatrampa rocas" })
    if (specie === "coalossal") moves.push({ name: "gigarroca ignea" })
    if (specie === "flapple") moves.push({ name: "gigacorrosion" })
    if (specie === "appletun") moves.push({ name: "giganectar" })
    if (specie === "sandaconda") moves.push({ name: "gigapolvareda" })
    if (specie === "toxtricity") moves.push({ name: "gigadescarga" })
    if (specie === "centiskorch") moves.push({ name: "gigacienfuegos" })
    if (specie === "hatterene") moves.push({ name: "gigacastigo" })
    if (specie === "grimmsnarl") moves.push({ name: "gigasopor" })
    if (specie === "alcremie") moves.push({ name: "gigacolofon" })
    if (specie === "copperajah") moves.push({ name: "gigatrampa acero" })
    if (specie === "duraludon") moves.push({ name: "gigadesgaste" })
    if (specie === "venusaur") moves.push({ name: "gigalianas" })
    if (specie === "blastoise") moves.push({ name: "gigacanonazo" })
    if (specie === "rillaboom") moves.push({ name: "gigarredoble" })
    if (specie === "cinderace") moves.push({ name: "gigaesfera ignea" })
    if (specie === "inteleon") moves.push({ name: "gigadisparo" })
    if (specie === "urshifu brusco") moves.push({ name: "gigagolpe brusco" })
    if (specie === "urshifu fluido") moves.push({ name: "gigagolpe fluido" })

    return moves

}